using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gra : MonoBehaviour
{
    public GameObject postac_gracza;
    //public GameObject prefabPostacGracza;
    // Start is called before the first frame update
    void Awake()
    {
        
        /*
        // SprawdŸ, czy prefabrykat zosta³ przypisany
        if (prefabPostacGracza != null)
        {
            // Tworzenie obiektu z prefabrykatu
            GameObject nowyObiekt = Instantiate(prefabPostacGracza);

            // Jeœli chcesz umieœciæ nowy obiekt pod innym obiektem, mo¿esz ustawiæ go jako rodzica
            nowyObiekt.transform.parent = transform;
        }
        else
        {
            Debug.LogWarning("Prefabrykat postaci gracza nie zosta³ przypisany.");
        }
        */
    }
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
